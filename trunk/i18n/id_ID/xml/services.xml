<!-- $Id: services.xml 283 2007-09-18 20:14:31Z danieldk $ -->

<chapter> <?dbhtml filename="services.html"?>
<title>Layanan jaringan</title>

<sect1>
<title>Super server internet</title>

<para>
Terdapat dua cara untuk menawarkan layanan TCP/IP: 
dengan menjalankan aplikasi server secara mandiri (standalone) 
sebagai daemon atau dengan menggunakan server super 
Internet, <command>inetd</command>. <command>inetd</command>
adalah sebuah daemon yang mengawasi sejumlah port. Jika sebuah klien 
mencoba untuk menghubungkan diri pada sebuah port <command>inetd</command> 
akan menangani koneksi dan mengirimkan koneksi pada perangkat lunak server 
yang menangani jenis koneksi tersebut. Keuntungan dari pendekatan ini adalah 
adanya tambahan sebuah layer keamanan dan membuatnya lebih mudah untuk 
mencatat log pada koneksi yang masuk. Kerugiannya adalah lebih lambat 
dibandingkan dengan daemon yang sifatnya standalone. Disarankan untuk 
menjalankan daemon secara standalone pada, misalnya server FTP yang memiliki beban yang tinggi.
</para>

<para>
<command>inetd</command> dapat dikonfigurasi menggunakan berkas
<filename>/etc/inetd.conf</filename>. Mari kita lihat pada contoh baris dari <filename>inetd.conf</filename>:
</para>

<screen>
# File Transfer Protocol (FTP) server:
ftp     stream  tcp     nowait  root    /usr/sbin/tcpd  proftpd
</screen>

<para>
Baris ini menyatakan bahwa <command>inetd</command> harus 
menerima koneksi FTP dan meneruskannya pada <command>tcpd</command>. Ini mungkin cukup aneh, 
karena <command>proftpd</command> biasanya menangani koneksi FTP. Anda juga bisa 
menyebutkan penggunaan <command>proftpd</command>
secara langsung pada <filename>inetd.conf</filename>, tetapi Slackware Linux biasanya 
mengirimkan koneksi pada <command>tcpd</command>. Program ini pada gilirannya mengirimkan 
koneksi pada <command>proftpd</command>, seperti yang ditentukan.
<command>tcpd</command> digunakan untuk memonitor layanan dan menyediakan kontrol akses berbasis host.
</para>

<para>
Layanan bisa dinonaktifkan dengan menambahkan karakter komentar (#) di awal baris. 
Disarankan untuk menonaktifkan semua layanan dan hanya mengaktifkan layanan 
yang Anda perlukan saja. Setelah mengganti
<filename>/etc/inetd.conf</filename> <command>inetd</command>
harus dijalankan kembali untuk mengaktifkan perubahan. Hal ini bisa dilakukan dengan mengirimkan sinyal HUP pada proses inetd:
</para>

<screen>
# <command>ps ax | grep 'inetd'</command>
   64 ?        S      0:00 /usr/sbin/inetd
# <command>kill -HUP 64</command>
</screen>

</sect1>

<sect1>
<title>Caching nameserver</title>

<para>
Sebuah caching nameserver menyediakan layanan DNS untuk sebuah mesin atau jaringan, tetapi tidak menyediakan DNS untuk sebuah 
domain. Ini berarti bahwa nameserver jenis ini hanya bisa digunakan untuk mengkonversi nama host menjadi alamat IP. Melakukan 
setting sebuah nameserver dengan Slackware Linux cukup mudah, karena BIND sudah terkonversi sebagai jenis ini secara default. 
Mengaktifkan caching nameserver hanya membutuhkan dua langkah: Anda harus menginstall BIND dan mengubah script inisialisasinya. 
BIND dapat diinstall dengan menambahkan paket BIND dari set disk <quote>n</quote>. Setelah itu, bind bisa dijalankan dengan 
menjalankan perintah
<command>named</command>. Jika Anda hendak menjalankan BIND secara default, ganti berkas <filename>/etc/rc.d/rc.bind</filename> 
menjadi bersifat executable. Hal ini bisa dilakukan dengan menjalankan perintah berikut sebagai root:
</para>

<screen>
# <command>chmod a+x /etc/rc.d/rc.bind</command>
</screen>

<para>
Jika Anda hendak menggunakan nameserver pada mesin yang menjalankan BIND, Anda harus mengubah <filename>/etc/resolv.conf</filename>.
</para>

</sect1>

<sect1>
<title>Server web Apache</title>

<para>
Apache adalah server web yang paling banyak digunakan pada Internet, dan tentu saja merupakan bagian dari Slackware Linux. 
Apache bisa diinstall dengan menambahkan paket <filename>apache</filename> dari set disk <quote>n</quote>. Jika Anda hendak 
menggunakan PHP, paket <filename>php</filename> (set disk <quote>n</quote>) dan <filename>mysql</filename> (set disk 
<quote>ap</quote>) juga diperlukan.
MySQL diperlukan, karena PHP bergantung pada pustaka MySQL. Anda tidak harus menjalankan MySQL. Setelah menginstall Apache
dapat dijalankan secara otomatis selagi boot dengan membuat berkas <filename>/etc/rc.d/rc.httpd</filename> executable. 
Anda bisa melakukan hal ini dengan menjalankan: 
</para>

<screen>
# <command>chmod a+x /etc/rc.d/rc.httpd</command>
</screen> 

<para>
Konfigurasi Apache bisa diubah pada berkas 
<filename>/etc/apache/httpd.conf</filename>. Apache bisa di
stop/start/restart setiap saat dengan perintah <command>apachectl</command>,
 digabung dengan parameter-parameter <parameter class="command">stop</parameter>, 
<parameter class="command">start</parameter> dan 
<parameter class="command">restart</parameter>. Sebagai contoh,
jalankan perintah berikut untuk me-restart Apache:
</para>

<screen>
# <command>apachectl restart</command>
/usr/sbin/apachectl restart: httpd restarted
</screen>

</sect1>

</chapter>
