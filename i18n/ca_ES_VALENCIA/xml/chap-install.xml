<chapter xmlns="http://docbook.org/ns/docbook" version="5.0"
	 xml:id="chap-install">
  <title>La instal·lació de Slackware Linux <!--Installing Slackware Linux--></title>

  <sect1 xml:id="chap-install-bootcd">
    <title>Llançar el CD d'instal·lació <!--Booting the installation CD-ROM--></title>

    <para>
		El mètode més senzill per a llançar el sistema d'instal·lació és
		mitjançant el CD-ROM d'instal·lació. Aquest és un CD arrencable,
		el que significa que la BIOS pot llançar el CD, de la mateixa 
		manera que pot llançar, per exemple, un disquet. La majoria dels
		sistemes moderns porten una BIOS que suporta la càrrega de CD-ROM.
		
      <!--The easiest method for booting the installation system is by
      using the installation CD-ROM. The Slackware Linux installation
      CD-ROM is a bootable CD, which means that the BIOS can boot the
      CD, just like it can boot, for example, a floppy disk. Most
      modern systems have a BIOS which supports CD-ROM booting.-->
    </para>

    <para>
		Si una vegada inserit el CD, no es carrega al engegar-se el sistema,
		normalment vol dir que la seqüència de llançament no està 
		configurada correctament a la BIOS. Accedeix a la configuració 
		de la BIOS (normalment açò es pot aconseguir pitjant la tecla
		&lt;Del&gt; o &lt;Esc&gt; quan apareix la pantalla de la BIOS).
		i assabentar-te que el CD-ROM està dalt de tot en la llista en 
		la seqüència de llançament. És possible que si estàs utilitzant 
		un CD-ROM SCSI tingues que configurar la seqüència a la BIOS de
		SCSI, enlloc de la BIOS del sistema. Per a més informació 
		consulta el manual de la targeta SCSI.
      <!--If the CD is not booted when you have the CD inserted in the
      CD-ROM drive during the system boot, the boot sequence is
      probably not correctly configured in the BIOS. Enter the BIOS
      setup (usually this can be done by holding the &lt;Del&gt; or
      &lt;Esc&gt; key when the BIOS screen appears) and make sure the
      CD-ROM is on the top of the list in the boot sequence.  If you
      are using a SCSI CD-ROM you may have to set the boot sequence in
      the SCSI BIOS instead of the system BIOS. Consult the SCSI card
      manual for more information.-->
    </para> 

    <para>
		Quan el CD-ROM es carrega, apareix una pantalla de pre-llançament.
		Normalment, es pot pitjar senzillament &lt;ENTER&gt; per a procedir
		a la càrrega del núcli Linux per defecte ((<filename>hugesmp.s
		</filename>). Este núcli requereix, almenys, una CPU Pentium PRO.
		Es pot llançar un núcli alternatiu introduïnt el nom del núcli
		al símbol del sistema i pitjant &lt;ENTER&gt;. La taula següent
		llista els diferents núclis disponibles al CD/DVD de Slackware 
		Linux.
      <!--When the CD-ROM is booted, a pre-boot screen will
      appear. Normally you can just press &lt;ENTER&gt; to proceed
      loading the default (<filename>hugesmp.s</filename>) Linux
      kernel. This kernel requires at least a Pentium Pro CPU. You can
      boot an alternative kernel by entering the kernel name on the
      prompt, and pressing &lt;ENTER&gt;.  The following table lists
      the different kernels that are available from the Slackware
      Linux CD or DVD.-->
    </para>

    <table xml:id="chap-install-bootcd-installkernels">
      <title>Núclis d'instal·lació<!--Installation kernels--></title>

      <tgroup cols="2">
	<thead>
	  <row>
	    <entry>Linux</entry><entry>Descripció</entry>
	  </row>
	</thead>
	<tbody>
	  <row>
	    <entry>huge.s</entry>
	    <entry>
			Abans, hi havia núclis específics per a les diferents 
			controladores de disc. Els nous núclis huge(enorme) inclouen
			suport per a totes les controlades ATA, SATA i SCSI comunes.
			Este núcli no incorpora suport per a SMP, i funciona en 
			processadors i486 i més moderns. Si tens una CPU Pentium Pro
			o més moderna, es recomana utilitzar el núcli <emphasis>
			hugesmp.s</emphasis>, fins i tot en sisteme amb només un 
			processador.
		  <!--Previously, there were specific kernels for different
	      sets of disk controlers. The new huge kernels include
	      support for all common ATA, SATA and SCSI
	      controllers. This kernel does not have SMP support, and
	      works on i486 and newer CPUs. If you have a Pentium Pro
	      or newer CPU, it is recommended to use the
	      <emphasis>hugesmp.s</emphasis> kernel, even on
	      uniprocessor systems.-->
	    </entry>
	  </row>
	  <row>
	    <entry>hugesmp.s</entry>
	    <entry>
			El núcli <emphasis>hugesmp.s</emphasis> té suport per a les
			controladores ATA, SATA i SCSI més comunes, a més, este núcli
			incorpora suport SMP. Aquest és el núcli recomanat amb CPUs
			Pentium PRO i més modernes.
	      <!--The <emphasis>hugesmp.s</emphasis> kernel has support
	      for common ATA, SATA, and SCSI
	      controllers. Additionally, this kernel has SMP
	      support. This is the recommended kernel on Pentium Pro
	      and newer CPUs.-->
	    </entry>
	  </row>
	  <row>
	    <entry>speakup.s</entry>
	    <entry>
			Aquest núcli és comparabla al <emphasis>huge.s</emphasis>,
			per afegeix suport per a  de maquinari de sistetitzadors de 
			veu.
	      <!--This kernel is comparable to the
	      <emphasis>huge.s</emphasis> kernel, but also includes
	      support for hardware speech synthesizers.-->
	    </entry>
	  </row>
	</tbody>
      </tgroup>
    </table>

    <para>
		Després de llançar el sistema d'instal·lació, se't preguntarà si
		estàs utilitzant un teclat especial (nacional) o no. Si tens un
		teclat US/International normal, el què és el més comú, pots
		pitja senzillament &lt;Enter&gt; en aquesta pregunta. Després
		d'això apareixerà la pantalla d'inici de sessió. Inicia-la com 
		<quote>root	</quote>, no et demanarà contrasenya. Després 
		d'iniciar sessió, s'engega l'interpret i ja pots començar a 
		instal·lar Slackware Linux. Aquest procés s'explica breument en
		aquest capítol.
		 
      <!--After booting the installation system, you will be asked whether
      you are using a special (national) keyboard layout or not. If
      you have a normal US/International keyboard, which is the most
      common, you can just press &lt;Enter&gt; at this question. After
      that the login prompt will appear. Log on as
      <quote>root</quote>, no password will be requested.  After
      login, the shell is started and you can begin installing
      Slackware Linux. The installation procedure will be explained
      briefly in this chapter.-->
    </para>
  </sect1>

  <sect1 xml:id="chap-install-partitioning">
    <title>Particionar un disc dur <!--Partitioning a hard disk--></title>

    <para>
		Instalar Slackware Linux requereix almenys una partició de Linux,
		a més es recomana crear una partició swap (intercanvi). Per a
		poder crear una partició ha d'haver espai lliure sense particionar
		al disc. Hi ha alguns programes que poden redimensionar particions.
		Per exemple, FIPS pot redimensionar particions FAT. Programes
		comercials com Partition Magic també poden redimensionar altres
		tipus de particions.
      <!--Installing Slackware Linux requires at least one Linux
      partition, creating a swap partition is also recommended. To be
      able to create a partition there has to be free unpartitioned
      space on the disk. There are some programs that can resize
      partitions. For example, FIPS can resize FAT partitions.
      Commercial programs like Partition Magic can also resize other
      partition types.-->
    </para>

    <para>
		Després de llançar la el CD-ROM Slackware Linux i de iniciar
		sessió tens dos programes a la teua disposició: <command>fdisk
		</command> i <command>cfdisk</command>. <command>cfdisk</command> 
		és el més senzill dels dos perque es controla mitjaçant una 
		interfície de menús. Aquesta secció descriu el programa
		<command>cfdisk</command>.
		
      <!--After booting the Slackware Linux CD-ROM and logging on, there
      are two partitioning programs at your disposal:
      <command>fdisk</command> and
      <command>cfdisk</command>. <command>cfdisk</command> is the
      easiest of both, because it is controlled by a menu
      interface. This section describes the <command>cfdisk</command>
      program.-->
    </para>

    <para>
		Per a particionar el primer disc dur pots, simplement, executar 
		<command>cfdisk</command>. En cas de que vulgues particionar
		algún altre disc o un disc SCSI, hauràs d'especificar quin disc
		vols particionar (<command>cfdisk /dev/device</command>). Els
		discs durs ATA s'identifiquen mitjançant la següent nomenclatura:
		<filename>/dev/hdn</filename>, amb <quote>n</quote> reemplaçat
		per un caràcter, per exemple. el <quote>mestre primari</quote> 
		es diu <filename>/dev/hda</filename>, l'<quote>esclau secundari
		</quote> es diu <filename>/dev/hdd</filename>. Els discs SCSI
		es diuen de la següent manera: <filename>/dev/sdn</filename>,
		canviant la <quote>n</quote> pel caràcter corresponent(el primer
		disc SCSI = a, el quart disc SCSI = d).
		
      <!--To partition the first harddisk you can simply execute
      <command>cfdisk</command>. If you want to partition another disk
      or a SCSI disk you have to specify which disk you want to
      partition (<command>cfdisk /dev/device</command>). ATA hard
      disks have the following device naming:
      <filename>/dev/hdn</filename>, with <quote>n</quote> replaced by
      a character, per exemple el <quote>mestre primari</quote> es diu
      <filename>/dev/hda</filename>, l'<quote>esclau secundari
      </quote> es diu <filename>/dev/hdd</filename>. Els discs SCSI
      es diuen de la següent manera: <filename>/dev/sdn</filename>,
      amb <quote>n</quote> substituït pel device character (the
      first SCSI disk = a, the fourth SCSI disk = d).-->
    </para>

    <figure xml:id="chap-install-partitioning-cfdisk">
      <title>La eina de particionat cfdisk</title>
      <mediaobject>
	<imageobject>
	  <imagedata fileref="../../images/cfdisk.png" format="PNG" width="100%" scalefit="1"/>
	</imageobject>
      </mediaobject>
    </figure>

    <para>
		Després d'iniciar <command>cfdisk</command> es mostren les 
		particions existens així com la quantitat d'espai lliure. La
		llista de particions pot ser navegada amb les tecles de fletxa 
		<quote>amunt</quote> i fletxa <quote>avall</quote>. Baix de tot
		de la pantalla es mostren algunes ordres, que es poden executar
		mitjançant la tecla &lt;Enter&gt;.
		
      <!--After starting <command>cfdisk</command>, the currently existing
      partitions are shown, as well as the amount of free space. The
      list of partitions can be navigated with the <quote>up</quote>
      and <quote>down</quote> arrow keys.  At the bottom of the screen
      some commands are displayed, which can be browsed with the
      <quote>left</quote> and <quote>right</quote> arrow keys.  A
      command can be executed with the &lt;Enter&gt; key.-->
    </para>

    <para>
		Pots crear una partició de Linux seleccionant <quote>Free
      Space</quote> (espai lliure) i executant la ordre <quote>New</quote>.
      <command>cfdisk</command> preguntará si el que vols crear és una
      partició primaria o lògica. Es poden crear un màxim de quatre 
      particions primàries i Linux es pot instal·lar tant en particions
      primàries con en lògiques. Si tens pensat intal·lar altres sistemes
      operatius a banda de Slackware Linux que requerisquen particions
      primàries, és una bona idea que instal·les Slackware Linux en un 
      lògica. El tipus de la nova partició es posa automàticament com 
      <quote>Linux Native</quote>, així que no és necessari posar-lo.
      
      <!--You can create a Linux partition by selecting <quote>Free
      Space</quote> and executing the <quote>New</quote>
      command. <command>cfdisk</command> will ask you whether you want
      to create a primary or logical partition.  The number of primary
      partitions is limited to four. Linux can be installed on both
      primary and logical partitions. If you want to install other
      operating systems besides Slackware Linux that require primary
      partitions, it is a good idea to install Slackware Linux onto a
      logical partition.  The type of the new partition is
      automatically set to <quote>Linux Native</quote>, so it is not
      necessary to set the partition type.-->
    </para>

    <para>
		La creació d'un partició d'intercanvi (swap) involucra els 
		mateixos passos que una partició normal de Linux, només que el
		tipus de la partició s'ha de canviar a <quote>Linux Swap</quote>
		després de crear la partició. El tamany recomanat per a la
		partició d'intercanvi dependrà de les teues necessitats. La 
		partició d'intercanvi s'utilitza per a emmagatzemar programes
		si la memòria principal (RAM) està plena. Si tens un disc dur
		d'un tamany raonable és bona idea crear-ne una de 256MB o 512MB,
		el que serà suficient per al funcionament normal. Després de
		crear la partició, es pot canviar el tipus de partició a 
		<quote>Linux Swap</quote> seleccionant la ordre <quote>Type</quote>,
		<command>cfdisk</command> et demanarà el nombre de tipus. A les
		partitions <quote>Linux Swap</quote> els correspon el tipus 
		número 82. Normalment el número 82 ja està triat, pel que pots
		seguir endavant pitjant la tecla &lt;Enter&gt;.
      
      <!--The creation of a swap partition involves the same steps as a
      normal Linux partition, but the type of the partition has to be
      changed to <quote>Linux Swap</quote> after the partition is
      created. The suggested size of the swap partition depends on
      your own needs. The swap partition is used to store programs if
      the main (RAM) memory is full. If you have a harddisk of a
      reasonable size, it is a good idea to make a 256MB or 512MB swap
      partition, which should be enough for normal usage.  After
      creating the partition, the partition type can be changed to
      <quote>Linux Swap</quote> by selecting the <quote>Type</quote>
      command. The <command>cfdisk</command> program will ask for the
      type number. <quote>Linux Swap</quote> partitions have type
      number 82.  Normally number 82 is already selected, so you can
      go ahead by pressing the &lt;Enter&gt; key.-->
    </para> 

    <para>
		Si estàs satisfet amb el particionament, pots alçar els canvis
		executant la ordre <quote>Write</quote>. Aquesta operació s'ha
		de confirmar introduïnt <command>yes</command> (sí). Després de
		alçar els canvis es pot deixar el <command>cfdisk</command> amb
		la ordre <command>Quit</command>. És una bona idea reiniciar
		l'ordinador abans d'iniciar la instal·lació, per a assabentar-nos
		de que els canvis en el particionament estàn actius. Pitja 
		&lt;ctrl&gt; + &lt;alt&gt; + &lt;del&gt; per a t el Linux
		i reiniciar l'ordinador.
      
      <!--If you are satisfied with the partitioning you can save the
      changes by executing the <quote>Write</quote> command. This
      operation has to be confirmed by entering
      <command>yes</command>. After saving the changes you can quite
      <command>cfdisk</command> with the <command>Quit</command>
      command. It is a good idea to reboot the computer before
      starting the installation, to make sure that the partitioning
      changes are active.  Press &lt;ctrl&gt; + &lt;alt&gt; +
      &lt;del&gt; to shut Linux down and restart the computer.-->
    </para>
  </sect1>

  <sect1 xml:id="chap-install-installing">
    <title>Instal·lant Slackware Linux</title>

    <para>
		L'instal·lador de Slackware Linux s'inicia executant <command>
		setup</command> a l'interpret del disc d'instal·lació. Setup
		mostrarà un menú amb vàries opcions. Pots veurer una captura de
		pantalla del instal·lador a <xref linkend="chap-install-installing-setup"/>. 
		Es requereixen completar totes les opcions per a fer una 
		instal·lació completa, però una vegada que has començat, el 
		programa et guiará a través d'elles.
      <!--The Slackware Linux installer is started by executing
      <command>setup</command> in the installation disk shell. Setup
      will show a menu with several choices.  You can see a screenshot
      of the installer in <xref
      linkend="chap-install-installing-setup"/>. Every
      option is required for a complete Slackware Linux installation,
      but once you start, the <command>setup</command> program will
      guide you through the options.-->
    </para>

    <figure xml:id="chap-install-installing-setup">
      <title>La eina setup <!--The setup tool--></title>
      <mediaobject>
	<imageobject>
	  <imagedata fileref="../../images/setup.png" format="PNG" width="100%" scalefit="1"/>
	</imageobject>
      </mediaobject>
    </figure>

    <para>
		La primera part de la instal·lació s'anomena <quote>ADDSWAP
		</quote>. La eina <command>setup</command> buscarà una partició
		del tipus <quote>Linux Swap</quote> i et preguntarà si vols 
		formatar-la i activar-la (mira la figura <xref
		linkend="chap-install-installing-addswap"/>). Normalment pots,
		senzillament, contestar <quote>Yes</quote>.
      
      <!--The first part of the installation is named
      <quote>ADDSWAP</quote>. The <command>setup</command> tool will
      look for a partition with the <quote>Linux Swap</quote> type,
      and ask you if you want to format and activate the swap
      partition (see figure <xref
      linkend="chap-install-installing-addswap"/>).
      Normally you can just answer <quote>Yes</quote>.-->
    </para>

    <figure xml:id="chap-install-installing-addswap">
      <title>Preparant la partició d'intercanvi <!--Setting up the swap partition--></title>
      <mediaobject>
	<imageobject>
	  <imagedata fileref="../../images/addswap.png" format="PNG" width="100%" scalefit="1"/>
	</imageobject>
      </mediaobject>
    </figure>

    <para>
		Després de preparar l'espai d'intercanvi, es llança el  menú 
		<quote>TARGET</quote>, que es pot veurer a <xref
		linkend="chap-install-installing-partselect"/>. Este apartat 
		acostuma a inicialitzar les particions de Slackware Linux. Setup 
		mostrarà totes les particions tipus <quote>Linux Native</quote>.
      
      <!--After setting up the swap space the <quote>TARGET</quote> menu
      is launched, which you can see in <xref
      linkend="chap-install-installing-partselect"/>. It is used to
      initialize the Slackware Linux partitions. Setup will display
      all partitions with the <quote>Linux Native</quote> type.-->
    </para>

    <figure xml:id="chap-install-installing-partselect">
      <title>Seleccionant una partició a inicialitzar <!--Selecting a partition to initialize--></title>
      <mediaobject>
	<imageobject>
	  <imagedata fileref="../../images/partselect.png" format="PNG" width="100%" scalefit="1"/>
	</imageobject>
      </mediaobject>
    </figure>

    <para>
		Després de triar una partició, la eina setup et preguntarà si 
		vols formatar la partició o no, i en eixe cas, si vols comprovar
		si el disc conté sectors defectuosos. (<xref
		linkend="chap-install-installing-fscheck"/>). La comprovació de
		disc pot prendre molt de temps.
      <!--After selecting one partition, the setup tool will ask whether
      you want to format a partition or not, and if you want to format
      it, whether you want to check the disk for bad sectors or not
      (<xref
      linkend="chap-install-installing-fscheck"/>). Checking
      the disk can take a lot of time.-->
    </para>

    <figure xml:id="chap-install-installing-fscheck">
      <title>Formatant la partició <!--Formatting the partition--></title>
      <mediaobject>
	<imageobject>
	  <imagedata fileref="../../images/fscheck.png" format="PNG" width="100%" scalefit="1"/>
	</imageobject>
      </mediaobject>
    </figure>

    <para>
		Després de triar que volem formatar una partició, es pot
		especificar quin sistema de fitxers s'hauria d'utilitzar. 
		Normalment, pots triar entre ext2, ext3 i reiserfs.	Ext2 va ser 
		el sistema de fitxers estàndard durant molt anys, pero no 
		suportava journaling (registres diaris). Un diari (journal) és
		un fitxer especial o una àrea de una partició en què es registren
		totes les operacions del sistema de fitxers. Quan el sistema
		té un accident, el sistema de fitxers es pot arreglar ràpidament,
		perque el núcli pot utilitzar el diari per a veurer quines 
		operacions de disc s'havien realitzat. Ext3 és el mateix 
		sistema de fitxers que ext2, però afig journaling. Reiserfs és 
		un sistema de fitxers que també	proporciona journaling, però 
		utilitza arbres balancejats, el que fa que moltes operacions
		sobre disc siguen més ràpides que amb Ext2 o Ext3, especialment
		al treballar amb molts fitxers menuts. El defecte és que al ser
		més nou, pot ser una miqueta més inestable.
		
      <!--After selecting to format a partition, you can specify which
      filesystem should be used (<xref
      linkend="chap-install-installing-filesystem"/>). Normally
      you can choose the ext2, ext3 and reiserfs filesystems. Ext2 was
      the standard Linux filesystem for many years, but it does not
      support journaling. A journal is a special file or area of a
      partition in which all filesystem operations are logged. When
      the system crashes, the filesystem can be repaired rapidly,
      because the kernel can use the log to see what disk operations
      were performed. Ext3 is the same filesystem as Ext2, but adds
      journaling. Reiserfs is a filesystem that also provides
      journaling. Reiserfs uses balanced trees, which make many
      filesystem operations faster than with Ext2 or Ext3, especially
      when working with many small files. A disadvantage is that
      Reiserfs is newer, and can be a bit more unstable.-->
    </para>

    <figure xml:id="chap-install-installing-filesystem">
      <title>Triant el tipus de sistema de fitxers <!--Selecting a filesystem type--></title>
      <mediaobject>
	<imageobject>
	  <imagedata fileref="../../images/filesystem.png" format="PNG" width="100%" scalefit="1"/>
	</imageobject>
      </mediaobject>
    </figure>

    <para>
		La primer partició inicialitzada es munta automàticament com 
		la partició arrel (/). Per a altres particions, es pot triar
		el punt de muntatge després de la inicialització. Tú pots, 
		per exemple, crear particions separades per a <filename>/</filename>,
		<filename>/var</filename>, <filename>/tmp</filename>,
		<filename>/home</filename> i <filename>/usr</filename>. Açò
		proporciona una protecció extra contra accidents. Com 
		<filename>/</filename> es canvia molt rarament després de la 
		instal·lació, si crees estes particions, la probabilitat de que
		estiguen enmig d'una operació d'escriptura durant un accident és
		molt menor. A més, és més segur crear un sistema de fitxers
		distint per a <filename>/home</filename> ja que si un programa
		té una vulnerabilitat, un usuari podria crear un enllaç dur al
		binari d'eixe programa sempre i quan el directori 
		<filename>/home</filename> està en el mateix sistema de fitxers 
		que les carpeta <filename>/{s}bin</filename>, 
		<filename>/usr/{s}bin</filename>, o <filename>/usr/local/{s}bin
		</filename>. En aquest cas, l'usuari continuarà podent accedir 
		al vell i vulnerable binari després de que el programa siga
		actualitzat.
      <!--The first initialized partition is automatically mounted as the
      root (/) partition. For other partitions the mount point can be
      selected after the initialization. You could, for example, make
      separate partitions for <filename>/</filename>,
      <filename>/var</filename>, <filename>/tmp</filename>,
      <filename>/home</filename> and <filename>/usr</filename>. This
      provides extra protection against crashes. Since
      <filename>/</filename> is rarely changed after the installation
      if you create these partitions, the chance that it will be in
      the middle of a write operation during a crash is much smaller.
      Besides that, it is safer to create a separate filesystem for
      <filename>/home</filename>. If a program has a security
      vulnerability, a user could create a hard link to the binary of
      that program, if the <filename>/home</filename> directory is on
      the same filesystem as the <filename>/{s}bin</filename>
      <filename>/usr/{s}bin</filename>, or
      <filename>/usr/local/{s}bin</filename> directories. This user
      will then still be able to access the old, vulnerable binary
      after the program is upgraded-->
    </para>

    <para>
		El següent pas és seleccionar el medi origen (<xref
      linkend="chap-install-installing-medium"/>). Este diàleg t'ofereix
      vàries opcions, com la instal·lació de Slackware Linux des d'un 
      CD-ROM o via NFS. La primera opció és la més comuna alhora 
      d'instal·lar Slackware Linux, pel que és el mètode que veurem ací.
      Després de triar <quote>CD-ROM</quote> se't demanarà si vols que 
      setup localitze el CD-ROM (<quote>Auto</quote>) o si per contra
      vols triar el dispositiu tú mateix (<quote>Manual</quote>). Si 
      tries <quote>Manual</quote> la eina setup et mostrarà una llista
      de dispositius, tria el dispositiu que conté el CD-ROM de Slackware
      Linux.
      
      <!--The next step is to select the source medium (<xref
      linkend="chap-install-installing-medium"/>). This
      dialog offers several choices, like installing Slackware Linux
      from a CD-ROM or installing Slackware Linux via NFS. Slackware
      Linux is usually installed from CD-ROM, so this is what we are
      going to look at. After selecting <quote>CD-ROM</quote> you will
      be asked whether you want to let setup look for the CD-ROM
      itself (<quote>Auto</quote>) or you want to select the CD-ROM
      device yourself (<quote>Manual</quote>). If you select
      <quote>Manual</quote> the setup tool will show a list of
      devices. Select the device holding the Slackware Linux CD-ROM.-->
    </para> 

    <figure xml:id="chap-install-installing-medium">
      <title>Triant el medi origen <!--Selecting the source medium--></title>
      <mediaobject>
	<imageobject>
	  <imagedata fileref="../../images/medium.png" format="PNG" width="100%" scalefit="1"/>
	</imageobject>
      </mediaobject>
    </figure>

    <para>
		Després de triar un origen de la instal·lació, la eina setup
		et demanarà de quins conjunt de discs (series) vols instal·lar 
		paquets (<xref linkend="chap-install-installing-disksets"/>). 
		Es llista una xicoteta descripció de capa conjunt de discs.
      <!--After choosing an installation source the setup tool will ask
      you which disk sets (series) you want to install packages from
      (<xref
      linkend="chap-install-installing-disksets"/>). A
      short description of each disk set is listed.-->
    </para>

    <figure xml:id="chap-install-installing-disksets">
      <title>Triant els conjunts de discs<!-- Selecting the disk sets--></title>
      <mediaobject>
	<imageobject>
	  <imagedata fileref="../../images/disksets.png" format="PNG" width="100%" scalefit="1"/>
	</imageobject>
      </mediaobject>
    </figure>

    <para>
		Pràcticament ha arribat l'hora de començar la instal·lació real. 
		La següent pantalla et demana què t'agradaria instalar. Les 
		opcions	més evidents són <quote>full</quote>, <quote>menu</quote>
		o <quote>expert</quote>. Si es selecciona <quote>full</quote> 
		instal·larà tots els paquets continguts als conjunts de discs
		seleccionats. Esta és la manera més senzilla d'instal·lar 
		Slackware Linux. L'inconvenient d'aquesta tria és que pot 
		ocupar molt d'espai de disc. La opció <quote>menu</quote>
		preguntarà quins paquets vols instal·lar de cada conjunt de
		discs. La opció <quote>expert</quote> és pràcticament igual que
		<quote>menu</quote>, només que et permet deseleccionar alguns 
		paquets realment importants del conjunt de discs <quote>a</quote>.
		
      <!--Now it is almost time to start the real installation. The next
      screen asks how you would like to install. The most obvious
      choices are <quote>full</quote>, <quote>menu</quote> or
      <quote>expert</quote>. Selecting <quote>full</quote> will
      install all packages in the selected disk sets. This is the
      easiest way of installing Slackware Linux.  The disadvantage of
      this choice is that it can take quite much disk space. The
      <quote>menu</quote> option will ask you for each disk set which
      packages you want to install. The <quote>expert</quote> option
      is almost equal to the <quote>menu</quote> option, but allows
      you to deselect some very important packages from the
      <quote>a</quote> disk set.-->
    </para>

    <para>
		Una vegada s'ha completat la instal·lació l'eina setup et
		permetrà configurar algunes parts del sistema. El primer diàleg
		et demanarà des d'on vodries instal·lar el núcli (mira <xref
		linkend="chap-install-installing-installkernel"/>). Normalment
		és una bona idea instal·lar el núcli des d'el CD-ROM de 
		Slackware Linux, açò seleccionarà el núcli amb el que la vas 
		instal·lar. Pots confirmar açò o triar un altre núcli.
	
		
      <!--After the completion of the installation the setup tool will
      allow you to configure some parts of the system. The first
      dialog will ask you where you would like to install the kernel
      from (see <xref
      linkend="chap-install-installing-installkernel"
      />). Normally it is a good idea to install the kernel from the
      Slackware Linux CD-ROM, this will select the kernel you
      installed Slackware Linux with. You can confirm this, or select
      another kernel.-->
    </para>

    <figure xml:id="chap-install-installing-installkernel">
      <title>Instal·lant el núcli<!--Installing the kernel--></title>
      <mediaobject>
	<imageobject>
	  <imagedata fileref="../../images/kernel.png" format="PNG" width="100%" scalefit="1"/>
	</imageobject>
      </mediaobject>
    </figure>

    <para>
		En aquest punt pots escollir fer una disc d'arranc (<xref
		linkend="chap-install-installing-bootdisk" />). És una bona idea 
		ja que el pots utilitzar en cas de que la configuració del LILO 
		es llance a perdre.
		
      <!--At this point you can choose to make a bootdisk (<xref
      linkend="chap-install-installing-bootdisk" />).  It
      is a good idea to make a bootdisk, you can use it to boot
      Slackware Linux if the LILO configuration is botched.-->
    </para>

    <figure xml:id="chap-install-installing-bootdisk">
      <title>Creant un disc d'arranc <!--Creating a bootdisk--></title>
      <mediaobject>
	<imageobject>
	  <imagedata fileref="../../images/bootdisk.png" format="PNG" width="100%" scalefit="1"/>
	</imageobject>
      </mediaobject>
    </figure>

    <para>
		El següent diàleg es pot utilitzar per a crear un enllaç, 
		<filename>/dev/modem</filename>, que apunta al teu dispositiu
		módem.(<xref
		linkend="chap-install-installing-modem" />). Si no tens un modem
		pots seleccionar <emphasis>no modem</emphasis>.
      <!--The following dialog can be used to make a link,
      <filename>/dev/modem</filename>, that points to your modem
      device. (<xref
      linkend="chap-install-installing-modem" />). If you
      do not have a modem you can select <emphasis>no
      modem</emphasis>.-->
    </para>

    <figure xml:id="chap-install-installing-modem">
      <title>Seleccionant el modem per defecte <!--Selecting the default modem--></title>
      <mediaobject>
	<imageobject>
	  <imagedata fileref="../../images/modem.png" format="PNG" width="100%" scalefit="1"/>
	</imageobject>
      </mediaobject>
    </figure>

    <para>
		El següent pas és triar si es vol utilitzar hotplug (<xref 
		linkend="chap-install-installing-hotplug"/>). Hotplug s'utilitza
		per a configurar automàticament dispositius endollables USB,
		PCMCIA i PCI. En línies generals, és una bona idea habilitat 
		la connexió en calent (hotplugging), però alguns sistemes poden
		tindre problemes amb ???el sondeig de guions d'endollat en calent.???
		
      The next step is to select whether you would like to use hotplug
      (<xref linkend="chap-install-installing-hotplug"
      />).  Hotplug is used for automatically configuring pluggable
      USB, PCMCIA and PCI devices. Generally speaking, it is a good
      idea to enable hotplugging, but some systems may have problems
      with the probing of the hotplug scripts.
    </para>

    <figure xml:id="chap-install-installing-hotplug">
      <title>Habilitant l'endollat en calent<!--Enabling hotplugging--></title>
      <mediaobject>
	<imageobject>
	  <imagedata fileref="../../images/hotplug.png" format="PNG" width="100%" scalefit="1"/>
	</imageobject>
      </mediaobject>
    </figure>

    <para>
		Els passos següents són importants ja que ajuden a instal·lar 
		LILO, el carregador de llançament del Linux. Llevat que tingues
		experiència configurant LILO és una bona idea triar la opció 
		<emphasis>simple</emphasis>, la qual intenta configurar LILO 
		automàticament (<xref linkend="chap-install-installing-liloinstall" />).
		
      <!--The following steps are important, the next dialogs will assist
      you with installing LILO, the Linux bootloader. Unless you have
      experience in configuring LILO it is a good idea to choose to
      use the <emphasis>simple</emphasis> option for configuration of
      LILO, which tries to configure LILO automatically (<xref
      linkend="chap-install-installing-liloinstall" />).-->
    </para>

    <figure xml:id="chap-install-installing-liloinstall">
      <title>Triant el tipus d'instal·lació de LILO <!--Selecting the kind of LILO installation--></title>
      <mediaobject>
	<imageobject>
	  <imagedata fileref="../../images/lilo.png" format="PNG" width="100%" scalefit="1"/>
	</imageobject>
      </mediaobject>
    </figure>

    <para>
		Després d'escollir la opció <emphasis>simple</emphasis> la
		utilitat de configuració del LILO et preguntarà si t'agradaria
		utilitzar un framebuffer o no (<xref
		linkend="chap-install-installing-framebuffer" />). Utilitzar
		un framebuffer et permetrà utilitzar la consola en diferents
		resolucions, amb unes dimensions diferents a la típica de 80x25
		cartàcters. Hi ha gent que utilitza molt la consola que prefereix
		utilitzar un framebuffer perque els permet tindre més text a la
		pantalla. Si no vols una consola framebuffer, o si no estàs segur,
		pots triar <emphasis>estàndard</emphasis> ací.
		
      <!--After selecting the <emphasis>simple</emphasis> option the LILO
      configuration utility will asks you whether you would like to
      use a framebuffer or not (<xref
      linkend="chap-install-installing-framebuffer" />).
      Using a framebuffer will allow you to use the console in several
      resolutions, with other dimensions than the usual 80x25
      characters. Some people who use the console extensively prefer
      to use a framebuffer, which allows them to keep more text on the
      screen. If you do not want a framebuffer console, or if you are
      not sure, you can choose <emphasis>standard</emphasis> here.-->
    </para>

    <figure xml:id="chap-install-installing-framebuffer">
      <title>Triant la resolució framebuffer <!--Choosing the framebuffer resolution--></title>
      <mediaobject>
	<imageobject>
	  <imagedata fileref="../../images/framebuffer.png" format="PNG" width="100%" scalefit="1"/>
	</imageobject>
      </mediaobject>
    </figure>

    <para>
		Després de posar el framebuffer li pots passar paràmetres extra
		al núcli (<xref
      linkend="chap-install-installing-kernel-param"
      />). Açò normalment no és necessari, així que si no vols passar-li
      paràmetre extra pots senzillament, pitjar la tecla &lt;Enter&gt;.
      
      <!--After setting the framebuffer you can pass extra parameters to
      the kernel (<xref
      linkend="chap-install-installing-kernel-param"
      />). This is normally not necessary, if you do not want to pass
      extra parameters you can just press the &lt;Enter&gt; key.-->
    </para>

    <figure xml:id="chap-install-installing-kernel-param">
      <title>Afegint paràmetres al núcli<!--Adding kernel parameters--></title>
      <mediaobject>
	<imageobject>
	  <imagedata fileref="../../images/kernel-param.png" format="PNG" width="100%" scalefit="1"/>
	</imageobject>
      </mediaobject>
    </figure>

    <para>
		L'últim pas de la configuració del LILO consisteix a seleccionar
		on volem que s'instal·le 
		(<xref linkend="chap-install-installing-lilo-dest"/>). l'MBR és
		el registre mestre de llançament (Master Boot Record), el 
		registre principal de llançament en PCs. Utilitza aquesta opció 
		si vols utilitzar Slackware Linux com l'únic Sistema Operatiu, o
		si vols emprar el LILO per a llançar altres sistemes operatius. 
		La opció <emphasis>Root</emphasis> instal·larà LILO en el registre
		de llançament de la partició <filename>/</filename> de Slackware
		Linux. Utilitza aquesta opció si utilitzes un altre carregador de
		llançament.
		
      <!--The last step of the LILO configuration is selecting where LILO
      should be installed (<xref
      linkend="chap-install-installing-lilo-dest"
      />). <emphasis>MBR</emphasis> is the master boot record, the
      main boot record of PCs. Use this option if you want use
      Slackware Linux as the only OS, or if you want to use LILO to
      boot other operating systems. The <emphasis>Root</emphasis>
      option will install LILO in the boot record of the Slackware
      Linux <filename>/</filename> partition. Use this option if you
      use another bootloader.-->
    </para>

    <figure xml:id="chap-install-installing-lilo-dest">
      <title>Escollint on s'hauria d'instal·lar el LILO <!--Choosing where LILO should be installed--></title>
      <mediaobject>
	<imageobject>
	  <imagedata fileref="../../images/lilo-dest.png" format="PNG" width="100%" scalefit="1"/>
	</imageobject>
      </mediaobject>
    </figure>

    <para>
		Ara se't demanarà que configures el ratolí. Tria el tipus de
		ratolí al diàleg que apareix (<xref
		linkend="chap-install-installing-mouse" />).
      <!--You will now be asked to configure your mouse. Select the mouse
      type from the dialog that appears (<xref
      linkend="chap-install-installing-mouse" />).-->
    </para>

    <figure xml:id="chap-install-installing-mouse">
      <title>Configurant un ratolí <!--Configuring a mouse--></title>
      <mediaobject>
	<imageobject>
	  <imagedata fileref="../../images/mouse.png" format="PNG" width="100%" scalefit="1"/>
	</imageobject>
      </mediaobject>
    </figure>

    <para>
		Aleshores se't preguntarà si el programa <command>gpm</command>
		hauria de carregar-se en l'arranc o no 
		(<xref linkend="chap-install-installing-ask-load-gpm" />).
		<command>gpm</command> és un daemon que permet tallar i enganxar
		a la consola.
      
      <!--You will then be asked whether the <command>gpm</command>
      program should be loaded at boot time or not (<xref
      linkend="chap-install-installing-ask-load-gpm" />).
      <command>gpm</command> is a daemon that allows you to cut and
      paste text on the console.-->
    </para>

    <figure xml:id="chap-install-installing-ask-load-gpm">
      <title>Escollint si GPM s'hauria d'iniciar<!--Choosing whether GPM should be started or not--></title>
      <mediaobject>
	<imageobject>
	  <imagedata fileref="../../images/ask-load-gpm.png" format="PNG" width="100%" scalefit="1"/>
	</imageobject>
      </mediaobject>
    </figure>

    <para>
		A continuació, amb uns pocs passos més,es configura la
		connectivitat de xarxa. Açò es un requeriment en pràcticament 
		tots els sistemes en xarxa. La instal·lació de Slackware Linux
		et preguntarà si vols configurar-la (<xref
		linkend="chap-install-installing-ask-configure-network"
		/>). Si contestes <quote>No</quote> pots saltar els passos 
		següents relatius a la xarxa.
      <!--The next few steps will configure network connectivity. This is
      required on almost every networked system. The Slackware Linux
      setup will ask you if you want to set up network connectivity
      (<xref
      linkend="chap-install-installing-ask-configure-network"
      />). If you answer <quote>No</quote> you can skip the next few
      network-related steps.-->
    </para>

    <figure xml:id="chap-install-installing-ask-configure-network">
      <title>Escollint si t'agradaria configurar la connectivatat de xarxa
      <!--Choosing whether you would like to configure network
      connectivity--></title>
      <mediaobject>
	<imageobject>
	  <imagedata fileref="../../images/ask-configure-network.png" format="PNG" width="100%" scalefit="1"/>
	</imageobject>
      </mediaobject>
    </figure>

    <para>
		Ara se't demanarà si vols posar el nom de màquina (<xref
		linkend="chap-install-installing-hostname" />). 
		Per favor tin en compte que aquest no és el nom de domini 
		plenament qualificat (FQDN, Fully Qualified Domain Name), només
		la part que representa a l'ordinador (normalment els caràcters
		abans del primer punt en un FQDN).
      <!--You will now be asked to set the hostname (<xref
      linkend="chap-install-installing-hostname" />).
      Please note that this is not the fully qualified domain name
      (FQDN), just the part that represents the host (normally the
      characters before the first dot in a FQDN).-->
    </para>

    <figure xml:id="chap-install-installing-hostname">
      <title>Posant el nom de host <!--Setting the host name--></title>
      <mediaobject>
	<imageobject>
	  <imagedata fileref="../../images/hostname.png" format="PNG" width="100%" scalefit="1"/>
	</imageobject>
      </mediaobject>
    </figure>

    <para>
		Després de posar el nom del host pots posar la part del domini
		del nom de domini plenament qualificat (FQDN) (<xref
		linkend="chap-install-installing-domainname" />).
      
      <!--After setting the host name you can set the domain name part of
      the fully qualified domain name (FQDN) (<xref
      linkend="chap-install-installing-domainname" />).-->
    </para>

    <figure xml:id="chap-install-installing-domainname">
      <title>Posant el nom de domini <!--Setting the domain name--></title>
      <mediaobject>
	<imageobject>
	  <imagedata fileref="../../images/domainname.png" format="PNG" width="100%" scalefit="1"/>
	</imageobject>
      </mediaobject>
    </figure>

    <para>
		La resta de passos per a configurar la xarxa depenen de si els
		nodes de la xarxa estàn configurats per a utilitzar una adreça
		IP estàtica o dinàmica. Algunes xarxes tenen un servidor DHCP 
		que assigna automàticament una adreça IP als nodes en la xarxa.
		Si aquest és el cas, tria <emphasis>DHCP</emphasis> durant el 
		pas d'instal·lació 
		(<xref linkend="chap-install-installing-set-ipconfig-type"
		/>). Quan es selecciona DHCP només se't preguntarà si s'ha 
		d'enviar un nom de màquina al servidor, normalment, pots deixar-ho 
		en blanc. Si utilitzes DHCP pots saltar-te la resta de la
		configuració de xarxa, descrita a continuació.
      <!--The rest of the network configuration steps depend on whether
      nodes in the network are configured to use a static or a dynamic
      IP address. Some networks have a DHCP server that automatically
      assigns an IP address to hosts in the network. If this is the
      case for the network the machine select
      <emphasis>DHCP</emphasis> during this step of the installation
      (<xref linkend="chap-install-installing-set-ipconfig-type"
      />). When DHCP is selected you will only be asked whether a host
      name should be sent to the server. Normally you can leave this
      blank. If you use DHCP you can skip the rest of the network
      configuration described below.-->
    </para>

    <para>
		Si la xara no té un servidor DHCP pots triar la opció
		<emphasis>static IP</emphasis>, que et permetrà posar manualment
		l'adreça IP i la resta de paràmetres relacionats.
      <!--If the network does not have a DHCP server you can choose the
      <emphasis>static IP</emphasis> option, which will allow you to
      set the IP address and related settings manually.-->
    </para>

    <figure xml:id="chap-install-installing-set-ipconfig-type">
      <title>Configuració manual o automàtica de l'adreça IP
      <!--Manual or automatic IP address configuration--></title>
      <mediaobject>
	<imageobject>
	  <imagedata fileref="../../images/set-ipconfig-type.png" format="PNG" width="100%" scalefit="1"/>
	</imageobject>
      </mediaobject>
    </figure>

    <para>
		El primer paso en la configuració manual és posar l'adreça IP
		de la primera interfície (eth0) de la màquina (<xref
		linkend="chap-install-installing-ipaddr" />).
      <!--The first step of the manual configuration is to set the IP
      address of the first interface (eth0) of the machine (<xref
      linkend="chap-install-installing-ipaddr" />).-->
    </para>

    <figure xml:id="chap-install-installing-ipaddr">
      <title>Posant l'adreça IP<!--Setting the IP addres--></title>
      <mediaobject>
	<imageobject>
	  <imagedata fileref="../../images/ipaddr.png" format="PNG" width="100%" scalefit="1"/>
	</imageobject>
      </mediaobject>
    </figure>

    <para>
		Una vegada posada l'adreça IP seràs preguntat per la màscara de
		subxarxa, que normalment depén de la ????classe???? d'adreça IP
      <!-- After setting the IP address you will be asked to enter the
      netmask.  The netmask is usually dependent on the IP address
      class -->(<xref
      linkend="chap-install-installing-netmask" />).
    </para>

    <figure xml:id="chap-install-installing-netmask">
      <title>Posant la màscara de subxarxa<!--Setting the netmask--></title>
      <mediaobject>
	<imageobject>
	  <imagedata fileref="../../images/netmask.png" format="PNG" width="100%" scalefit="1"/>
	</imageobject>
      </mediaobject>
    </figure>

    <para>
		Aleshores se't demanarà que poses l'adreça de la porta d'enllaç
		(gateway) (<xref linkend="chap-install-installing-gateway" />).
		La porta d'enllaç és la màquina en la xarxa que proporciona 
		accés a altres xarxes mitjançant l'enrutatge de paquets. Si la 
		teua xarxa no té porta d'enllaç pots, senzillament, colpejar la 
		tecla &lt;ENTER&gt;.
		
      <!--You will then be asked to set the address of the gateway (<xref
      linkend="chap-install-installing-gateway" />). The
      gateway is the machine on the network that provides access to
      other networks by routing IP packets. If your network has no
      gateway you can just hit the &lt;ENTER&gt; key.-->
    </para>

    <figure xml:id="chap-install-installing-gateway">
      <title>Posant la porta d'enllaç<!--Setting the gateway--></title>
      <mediaobject>
	<imageobject>
	  <imagedata fileref="../../images/gateway.png" format="PNG" width="100%" scalefit="1"/>
	</imageobject>
      </mediaobject>
    </figure>

    <para>
		El diàleg següent et preguntarà si vols utilitzar un servidor de
		noms (<xref linkend="chap-install-installing-ask-nameserver"/>).
		Un servidor de noms és un servidor que pot proporcionar l'adreça
		IP associada a un nom de màquina. Per exemple, si naveges fins a
		<emphasis>www.slackbasics.org</emphasis>, el servidor de noms
		<quote>convertirà</quote> el nom <emphasis>www.slackbasics.org
		</emphasis> a la seua adreça IP corresponent.
      <!--The next dialog will ask you whether you want to use a
      nameserver or not (<xref
      linkend="chap-install-installing-ask-nameserver"
      />). A nameserver is a server that can provide the IP address
      for a hostname. For example, if you surf to
      <emphasis>www.slackbasics.org</emphasis>, the nameserver will
      <quote>convert</quote> the
      <emphasis>www.slackbasics.org</emphasis> name to the
      corresponding IP address.-->
    </para>

    <figure xml:id="chap-install-installing-ask-nameserver">
      <title>Triant si vols emprar un servidor de noms o no 
      <!--Choosing whether you want to use a nameserver or not--></title>
      <mediaobject>
	<imageobject>
	  <imagedata fileref="../../images/ask-nameserver.png" format="PNG" width="100%" scalefit="1"/>
	</imageobject>
      </mediaobject>
    </figure>

    <para>
		Si vas escollir utilitzar un servidor de noms, se't donarà la oportunitat
		de posar la seua adreça IP (<xref
		linkend="chap-install-installing-nameserver" />).
      <!--If you chose to use a nameserver, you will be given the
      opportunity to set the IP address of the nameserver (<xref
      linkend="chap-install-installing-nameserver" />).-->
    </para>

    <figure xml:id="chap-install-installing-nameserver">
      <title>Posant el(s) servidor(s) de noms<!--Setting the nameserver(s)--></title>
      <mediaobject>
	<imageobject>
	  <imagedata fileref="../../images/nameserver.png" format="PNG" width="100%" scalefit="1"/>
	</imageobject>
      </mediaobject>
    </figure>

    <para>
		La última pantalla de configuració de xarxa en mostra un resum
		dels paràmetres triats, oferint-nos la oportunitat de corregir 
		aquells paràmetres que no siguen correctes (<xref
		linkend="chap-install-installing-network-confirm"/>).
      <!--The final network settings screen provides an overview of the
      settings, giving you the opportunity to correct settings that
      have errors (<xref
      linkend="chap-install-installing-network-confirm"
      />).-->
    </para>

    <figure xml:id="chap-install-installing-network-confirm">
      <title>Confirmant els paràmetres de xarxa <!--Confirming the network settings--></title>
      <mediaobject>
	<imageobject>
	  <imagedata fileref="../../images/network-confirm.png" format="PNG" width="100%" scalefit="1"/>
	</imageobject>
      </mediaobject>
    </figure>

    <para>
		Després de la configuració de xarxa pots posar quins serveis
		s'haurien de iniciar (<xref
		linkend="chap-install-installing-startup-services"
		/>). Pots marcar o desmarcar servicis amb la tecla &lt;SPACE&gt;.
      
      <!--After the network configuration you can set which services
      should be started (<xref
      linkend="chap-install-installing-startup-services"
      />). You can mark/unmark services with the &lt;SPACE&gt; key.-->
    </para>

    <figure xml:id="chap-install-installing-startup-services">
      <title>Habilitant/deshabilitant serveis d'inici <!--Enabling/disabling startup services--></title>
      <mediaobject>
	<imageobject>
	  <imagedata fileref="../../images/startup-services.png" format="PNG" width="100%" scalefit="1"/>
	</imageobject>
      </mediaobject>
    </figure>

    <!-- try fonts? -->

    <para>
		Tradicionalment en sistemes tipus UNIX el rellotge del sistema 
		es posa amb la zona horària UTC. Si este és el cas, tria 
		<emphasis>Yes</emphasis> al pas següent (<xref
		linkend="chap-install-installing-ask-clock-utc"
		/>). Si a més utilitzes un SO que no siga tipus UNIX en el
		mateix sistema, com Windows, sol ser bona idea escollir 
		<emphasis>No</emphasis>, perque alguns sistemes operatius de PC
		no treballen amb dos rellotges separats, un de sistema i un de 
		programari.
      <!--Traditionally the system clock is set to the UTC timezone on
      UNIX(-like) systems. If this is the case, select
      <emphasis>Yes</emphasis> during the next step (<xref
      linkend="chap-install-installing-ask-clock-utc"
      />). If you also use a non-UNIX(-like) OS on the same system,
      like Windows, it is usually a good idea to choose
      <emphasis>No</emphasis>, because some PC operating systems do
      not work with a separate system clock and software clock.-->
    </para>

    <figure xml:id="chap-install-installing-ask-clock-utc">
      <title>Triant si el rellotge del sistema es posa UTC 
      <!--Choosing whether the clock is set to UTC--></title>
      <mediaobject>
	<imageobject>
	  <imagedata fileref="../../images/ask-clock-utc.png" format="PNG" width="100%" scalefit="1"/>
	</imageobject>
      </mediaobject>
    </figure>

    <para>
		Aleshores se't donarà la oportunitat de triar la zona horària
		You will then be given the opportunity to select the time zone
		(<xref linkend="chap-install-installing-timezone"
		/>). Açò és especialment important en sistemes que tenen el seu
		rellotge del sistema posat a UTC, ja que si no es selecciona 
		la zona horària correcta el rellotge de programari no coincidirà
		amb l'hora local.
		
      <!--You will then be given the opportunity to select the time zone
      (<xref linkend="chap-install-installing-timezone"
      />). This is especially important on systems that have their
      system clock set to UTC, without selecting the correct timezone
      the software clock will not match the local time.-->
    </para>

    <figure xml:id="chap-install-installing-timezone">
      <title>Posnat la zona horària <!--Setting the timezone--></title>
      <mediaobject>
	<imageobject>
	  <imagedata fileref="../../images/timezone.png" format="PNG" width="100%" scalefit="1"/>
	</imageobject>
      </mediaobject>
    </figure>

    <para>
		Si has instal·lat el Sistema de Finestres X, ara pots posar el 
		gestor de finestres per defecte (<xref
		linkend="chap-install-installing-defaultwm"
		/>). La funcionalitat més senzilla d'un gestor de finestres és 
		proporcionar una gestió bàsica de finestres, com barres de títol,
		però algunes opcions, com KDE, proporcionen un entorn d'escriptori
		complet.
      <!--If you installed the X Window System you can now set the default
      window manager (<xref
      linkend="chap-install-installing-defaultwm"
      />). The most basic functionality of a window manager is to
      provide basic window managing functionality like title bars. But
      some options, like KDE, provide a complete desktop environment.-->
    </para>

    <figure xml:id="chap-install-installing-defaultwm">
      <title>Escollint el gestor de finestres per defecte 
      <!--Choosing the default window manager--></title>
      <mediaobject>
	<imageobject>
	  <imagedata fileref="../../images/defaultwm.png" format="PNG" width="100%" scalefit="1"/>
	</imageobject>
      </mediaobject>
    </figure>

    <para>
		L'últim pas és posar la contrasenya de root (<xref
      linkend="chap-install-installing-rootpasswd" />).
      Setup et demanarà si t'agradaria posar una contrasenya o no. No hi
      ha cap raó per a fer açò, i sense una contrasenya de root el teu
      sistema és perillosament insegur.
      <!--The final step is to set the root password (<xref
      linkend="chap-install-installing-rootpasswd" />).
      The setup will ask you whether you would like to set it or
      not. There is no reason not to do this, and without a root
      password your system is dangerously insecure.-->
    </para>

    <figure xml:id="chap-install-installing-rootpasswd">
      <title>Posant la contrasenya de root<!--Setting the root password--></title>
      <mediaobject>
	<imageobject>
	  <imagedata fileref="../../images/rootpasswd.png" format="PNG" width="100%" scalefit="1"/>
	</imageobject>
      </mediaobject>
    </figure>

    <para>
		En aquest punt ja has completat la instal·lació de Slackware Linux.
		Ara pots reiniciar el sistema per a iniciar el teu recentment 
		instal·lat sistema Slackware Linux. No ha sigut difícil, 
		no és així? ;)
      <!--At this point you have completed the Slackware Linux
      installation. You can now reboot the system to start your fresh
      new Slackware Linux system. It wasn't that hard, was it? ;-)-->
    </para>

    <figure xml:id="chap-install-installing-finished">
      <title>Finalitzat<!--Finished--></title>
      <mediaobject>
	<imageobject>
	  <imagedata fileref="../../images/finished.png" format="PNG" width="100%" scalefit="1"/>
	</imageobject>
      </mediaobject>
    </figure>
  </sect1>
</chapter>
